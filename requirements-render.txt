# Minimal deps for Render: dashboard + /data/* + health (no Selenium / Playwright / Chrome).
# Build: pip install -r requirements-render.txt
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0
pandas>=2.0.0
openpyxl>=3.1.0
psycopg2-binary>=2.9.0
beautifulsoup4>=4.12.0
razorpay>=1.3.0
