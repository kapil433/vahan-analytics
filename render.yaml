# Render Blueprint — https://dashboard.render.com/ → New → Blueprint
# Or: Web Service → connect repo → Build: pip install -r requirements-render.txt
#     Start: uvicorn api.main:app --host 0.0.0.0 --port $PORT
services:
  - type: web
    name: vahan-intelligence-api
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements-render.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz/warm
    envVars:
      # ── Optional: PostgreSQL (leave unset to use bundled SQLite) ─────────────
      # - key: DATABASE_URL
      #   sync: false         # set to your Render PostgreSQL Internal URL
      # ── Optional: CORS allow-list ────────────────────────────────────────────
      - key: CORS_ALLOW_ORIGINS
        value: https://www.vahanintelligence.in,https://vahanintelligence.in,https://kapil433.github.io
      # ── HSTS is on by default; set to 1 only on a non-https pre-prod env ────
      # - key: DISABLE_HSTS
      #   value: "1"

# (Razorpay / JWT env vars removed — Vahan Intelligence is free for everyone in v2.)
